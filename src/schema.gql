# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Passenger {
  firstName: String!
  lastName: String!
  ticket: Ticket!
  ticketNumber: String
}

type Query {
  tickets: [Ticket!]!
  users: [UserEntity!]!
  usersByQuery(query: String!): [UserEntity!]!
}

type Ticket {
  categoryId: Float!
  changeReason: String
  datesAndItineraries: [String!]
  description: String
  estimatedTickets: Float
  id: ID!
  last_reply: DateTime
  officeId: Float
  passengers: [Passenger]
  pnr: String
  priority: TicketPriority!
  refundReason: String
  status: String!
  subcategoryId: Float!
  subject: String
}

enum TicketPriority {
  HIGH
  LOW
  MEDIUM
  URGENT
}

type UserEntity {
  contactNumber: String
  email: String!
  first_name: String!
  id: ID!
  last_name: String!
  profilePicture: String
  role: UserRole!
}

enum UserRole {
  ADMIN
  CLIENT
}